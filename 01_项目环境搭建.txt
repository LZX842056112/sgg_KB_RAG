
1. 项目创建和依赖同步
  1.1 安装uv 并配置到path环境变量
  1.2 可以配置uv的全局镜像 [可选]
  1.3 粘贴项目的依赖文件 pyproject.toml  uv.lock
  1.4 同步依赖 uv sync
  1.5 查看依赖情况 uv tree
  1.5 了解uv的相关命令
      uv  add   依赖
      uv pip install 依赖 [制定下载的地址 --index-url xxxx124]
      uv sync
      uv lock

2. 导入项目的配置文件
   .env

3. 导入项目的日志工具和配置
   粘贴代码即可  app/core/logger.py
   大致理解日志工具的作用和级别

4. 导入项目对应提示词文件和加载工具
   prompts -> app /
   local_prompt.py -> app/core

5. 批量导入工具类
6. 导入doc文件

下午回来: 准备两份 准备两个gitee的地址

7. 关联gitee的过程
  gitee创建一个开源的仓库 (可视化地址 粘贴到进度手册 | 使用提交地址进行关联)
  创建项目的忽略文件 [plugins / .ignore  / 项目 右键 new ignore file -> git -> python]
  version control -> git create ... -> 红色 -> commit 注意提交的描述清晰无误
  关联远程仓库 项目 右键 git  manage remotes ...
  向远程推送了

目标:
  1. 项目依赖 git 地址 两套
  2. 主图+测试
  3. 第一个节点+测试
